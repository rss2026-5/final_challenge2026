trajectory_planner:
  ros__parameters:
    odom_topic: "/odom"
    map_topic: "/map"

trajectory_follower:
  ros__parameters:
    odom_topic: "/odom"
    drive_topic: "/drive"

safety_controller:
  ros__parameters:
    scan_topic: "/scan"
    safety_topic: "/vesc/low_level/input/safety"

overall_controller:
  ros__parameters:
    odom_topic: "/odom"
    drive_topic: "/drive"
    goal_arrival_threshold: 1.0
    parking_duration: 5.0
    detection_window: 3.0

object_detector:
  ros__parameters:
    camera_topic: "/zed/rgb/image_rect_color"

traffic_light_detector:
  ros__parameters:
    camera_topic: "/zed/rgb/image_rect_color"
